import PetHealthModel from "./pet-health.schema";

export const createService = async (payload) => {
  try {
    console.log("payload", payload);
  } catch (error) {
    console.log("errr", error);
  }
};
